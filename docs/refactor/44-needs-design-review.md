# 44 — 디자인/제품 확인 필요 목록 (4단계)

- 작성일: 2026-07-27
- **이 목록은 코드 작업 전에 사람이 봐야 한다** — 어느 쪽으로 맞추든 화면이 눈에 띄게 바뀐다.
- `rd-charts-DESIGN.md`에는 아래 항목의 값 규정이 없음을 확인했다(디자인 스펙 근거 부재 →
  제품 결정 필요). 결정 전까지 해당 항목 구현은 보류하고, 결정되면 이 표에 기록한다.

| # | 항목 | 현재 iOS | 현재 AOS | 잠정 권고 | 결정 |
|---|---|---|---|---|---|
| D1 | **페이스 문자열 표기** | `4'30"` (분'초") / 무효 `-'--"` | `5'30''` (분'초'') / 무효 `-'--''` / 상한 99분 | iOS 표기(`'`+`"`) — 타이포그래피상 표준 기호이자 짧음. 상한 99분은 채택 | **iOS 표기 + 99분 상한** (2026-07-28, 권고 채택) |
| D2 | **2점 페이스 계산식 병합** | 워치 speed 우선, 게이트 없음, floorToDecimal(2) | Haversine 폴백, 1~41km/h 게이트, float 연산 | 병합: 워치 speed 우선 + 누적델타≤0 시 Haversine 폴백 + 게이트 채택(이상치 방어) + Double 정밀도 + 내림 제거(코어 필터가 방어) — **양 앱 모두 숫자가 미세 변동** | **병합안 채택** (2026-07-28, 양 앱 숫자 미세 변동 고지 후 확정) |
| D3 | **최대심박 성별 불명** | 여성 공식(206−0.88a) | 남성 공식(220−a) | UNKNOWN 전용 규칙 명시 필요. 권고: 보수적으로 낮은 쪽(여성 공식) — 존5 과대평가 방지. **AOS 사용자 화면 변화** | **여성 공식(206−0.88a)** (2026-07-28, AOS 성별 불명 사용자 화면 변화 고지 후 확정) |
| D4 | **segmentCount** | 고정 5 | floor(총거리단위), 상한 120, 시간모드 0 | AOS 방식(거리 비례) — 구간 통계의 의미(km 랩)에 부합. **iOS 스크럽 랩 값 변화** | 대기 |
| D5 | 등장 애니 이징·기본값 | .easeOut / 플래그 | EmphasizedDecelerate / 라인 기본 on | iOS easeOut 통일 + 기본 off 통일 | 대기 |
| D6 | 다크 테마 | 시스템 추종 | 라이트 고정 | 제품 결정(AOS 다크 지원 여부) — SDK는 양쪽 지원이므로 앱 정책 | 대기 |
| D7 | 도넛 히트 대역 | 링 폭 그대로 | max(링, 48dp) | AOS 방식(접근성 우위)을 SDK 기본으로 — iOS 탭 영역 넓어짐(시각 무변화) | 대기 |
| D8 | 색바 범례 샘플 수 | 24 | 40 | 40 (부드러움) 또는 `legendStops(count)` 인자화로 무결정 통과 | 대기 |
| D9 | 접근성 라벨 정책 | 도넛만, 선택분 낭독 | 3차트, 전체 분포 낭독(한국어 하드코딩) | 코어 기본 문자열 + 앱 주입. 낭독 범위는 AOS안(전체 분포) 권고 | 대기 |
| D10 | 롱프레스 임계 | 0.5s 고정 | 시스템 설정 연동 | 플랫폼 관례 존중(현행 유지) — 동등성 대상 아님(T3)으로 확정 기록 | 대기 |
| D11 | HR존 dt 폴백(2021 이전 기록) | exerciseTime 델타+폴백 | 델타 단일(폴백 없음) | iOS 폴백 규칙을 코어 채택 — 구 기록 보유 사용자의 AOS 화면 변화 가능 | 대기 |

주: D2·D3·D4는 "재정렬만으로 사라지는 차이"가 아니라 **기준 선택이 곧 제품 변경**인 항목이다.
잠정 권고로 구현을 준비하되, 릴리스 전 확인을 강제한다(체크리스트 50 문서).
